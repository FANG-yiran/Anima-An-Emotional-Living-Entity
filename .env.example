# ========== LLM Agent 配置（OpenAI 兼容接口） ==========
# 支持 DeepSeek / 通义千问 / Kimi / OpenAI / 本地代理等任何 OpenAI 兼容服务。
# 未配置 Key 时，系统自动回退到内置规则引擎，网页仍可完整运行。
#
# 使用方法：复制本文件为 .env 并填写真实值（.env 已被 .gitignore 忽略）。
# 请不要把真实 Key 填写在本文件中，以免被提交到版本库。

# 必填：你的 API Key
OPENAI_API_KEY=

# 可选：接口 Base URL（默认 https://api.openai.com/v1）
# DeepSeek 示例: https://api.deepseek.com/v1
# 通义千问示例: https://dashscope.aliyuncs.com/compatible-mode/v1
# Kimi 示例:    https://api.moonshot.cn/v1
OPENAI_BASE_URL=

# 可选：模型名（默认 gpt-4o-mini）
# DeepSeek 示例: deepseek-chat
OPENAI_MODEL=
